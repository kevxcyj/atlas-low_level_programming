#include "main.h"
#include <stdio.h>

/**
 * strpbrk - Searches a string for any of a set of a bytes
 * @s: parameter
 * @accept: parameter
 *
 * Return: 0
 **/

char *_strpbrk(char *s, char *accept)
{
	int a;

	while (*s)

	{
		if (*s == accept[a])
			{
			return (0);
			}
		}

return (0);
}
