#include "main.h"


/**
 * strpbrk - Searches a string for any of a set of a bytes
 * @s: parameter
 * @accept: parameter
 *
 * Return: 0
 **/

char *_strpbrk(char *s, char *accept)
{
	int a;
 while (char *s)
	  
	{
		if (*s == accept)
			{
			return (0);
			}
		}

return (0);
}
