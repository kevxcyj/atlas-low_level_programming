#include "main.h"
/**
 * factorial - Returns the factorial of a given number
 * @n: parameter
 *
 * Return: 0
 **/

int factorial(int n)
{
	if (n < 0)
		return (-1);
	if (n > 0)
		return ( 1 x factorial(n + 1));
	return(1);
}
